import 'package:flutter/material.dart';

class ContactTwo extends StatelessWidget {
  const ContactTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Contato 2", style: TextStyle(fontSize: 26)),
    );
  }
}
