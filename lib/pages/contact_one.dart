import 'package:flutter/material.dart';

class ContactOne extends StatelessWidget {
  const ContactOne({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Contato 1", style: TextStyle(fontSize: 26)),
    );
  }
}
